Index: src/disk.c
--- src/disk.c.orig
+++ src/disk.c
@@ -7,7 +7,6 @@
 
 #include "fix_coverity.h"
 
-#include <asm/byteorder.h>
 #include <errno.h>
 #include <fcntl.h>
 #include <inttypes.h>
@@ -159,7 +158,7 @@ msdos_disk_get_partition_info (int fd, int write_signa
 	} else if (num == 0) {
 		/* Whole disk */
 		*start = 0;
-		ioctl(fd, BLKGETSIZE, &disk_size);
+		//ioctl(fd, BLKGETSIZE, &disk_size);
 		*size = disk_size;
 	} else if (num >= 1 && num <= 4) {
 		/* Primary partition */
